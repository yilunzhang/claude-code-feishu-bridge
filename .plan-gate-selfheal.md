# plan v1.5.0:版本漂移自愈 + 停摆必通知(codex r1 收敛后)

## 起因(今天的真实故障,2026-07-30)

lark-cli 被升级 **1.0.66 → 1.0.80**(非 Yilun 主动)→ `FingerprintGate` 精确比对不符 →
`outbound_gate=degraded:version_mismatch` → **全部出站静默停摆**;入站不过此门 →
症状 = "我发的能进来、转发出不去",积压 27 条。**Yilun 靠「怎么没转发了」发现,桥一声没吭。**
恢复靠人肉 `bridgectl doctor --chat-id <群>`(不可能记得)。
**notify / StopFailure 告警也走同一个门** = 报警器与被报警物共用一条线。

## Yilun 的决定
A 方案(自动自检重钉,失败才停摆,**不删检查**)· **停摆必须通知,不能静默** ·
自检消息发 **e2e 测试群** `oc_ef148370df62f0a61e113731c6c50eb3`。

## 定位与假设(别 overdesign)
个人轻量工具、单用户、群内可信、daemon 后台单进程、低频。
**不要**:消息队列、监控平台、告警框架、健康检查服务、配置中心、重试框架、线程超时器。

---

## 🔴 codex r1 抓到的最关键一条:自检 oracle 测错了东西

**实测确认**:`ctl.doctor` 自检用 **`--text`**(`ctl.py:665`),而真实转发用 **`--markdown`**
(`outbound.py:383`)——**两条不同代码路径**。

**这不是理论问题:今天我跑 doctor 重钉 1.0.80,验证的是 `--text` 能用,却据此放行了
`--markdown`。测了零件没测装配。** 而 v1.4.1 修的恰恰就是 markdown 渲染 = 自检没覆盖的那条路。

→ **自检必须用与 `session_turn` 完全相同的形状**:`+messages-send --markdown`,
并要求同样的 `ok` + `data.message_id` 契约。

**并砍掉 recall**(codex):recall 证明不了正常出站,反而制造两种错判 ——
DELETE 坏了会误停全部出站,而 recall 好了不代表 `--markdown` 好。
测试消息留在 e2e 群即可(已有纪律:e2e 消息无需善后)。

**残余风险(记档,不做)**:卡片/reaction/post 各自可能独立回归 —— 不建兼容性矩阵。

### 🔑 本轮换掉的安全性质(codex r2 点名,必须记档)
**false-green admission**:旧行为**从不信任未钉版本的 CLI**;自愈会在**一次 markdown 发送
拿到 `message_id`** 后就重钉放行 —— **即便渲染细节已错、或卡片/reaction/post 已坏**。
这是用"自动恢复"换掉的东西,不是纸面风险。接受理由:单一 oracle 覆盖**主路径**
(session_turn 转发 = 绝大多数出站),其余路径各自有失败处理;个人工具,停摆的代价
(静默丢消息 + 人肉 doctor)已被实测证明比这个残余风险更痛。

---

## 改动

### 1. 自检群 = 硬编码常量(codex:砍掉 `selfcheck_chat_id` 配置项)
`constants.SELFCHECK_CHAT_ID = "oc_ef148370df62f0a61e113731c6c50eb3"`。
个人部署、就一个群,加配置项是过度设计。**绝不回退到工作群**(会留痕)。

### 2. `lib/fingerprint.py` —— 自愈放 `tick()`,不放 `startup()`/`_evaluate()`
**codex M4 实证**:`startup()` 在 daemon 就绪前跑(`daemon.py:60`),supervisor 约 52s 就会判
失败;而原设计串起来可达 130s。→
- `startup()` **只检测、不联网**,照常置 degraded、让 daemon 正常就绪;
- 首个 `tick()` 才做自愈。**同步跑是安全的**:已核实 `loop_iteration` 第一件事就是写心跳
  (`daemon_core.py:355`),`gate.tick()` 在其后(:369),接管阈值 180s
  (`HUNG_THRESHOLD_MS`);砍掉 recall 后只剩一次有界 send,远低于阈值。
- **不加线程外层超时**(codex:超时线程会让 doctor 继续跑、之后仍可能写 config)。用现有
  subprocess timeout。

**自愈期间先把门写成 `degraded:version_selfcheck`**(codex M4):否则 notify/StopFailure
这类**直发**路径会读到旧的 `ok`,在未验证的 CLI 上发出去。

### 3. 「每版本只自愈一次」必须**持久化**(codex M5)
进程内变量在 daemon 重启即失忆。实测重启很常见(ensure_daemon / code-identity 切换 / 崩溃)。
codex 估算:崩溃循环下最坏约 **360 次/小时** → 测试群被刷屏。
→ **联网前**先把 `pinned→actual` 写进 `daemon_state`(现有表,零迁移)。
**手动 `bridgectl doctor` 忽略该标记**(留人工恢复路径)。

### 4. 通知(硬要求:不能静默)
**已实测**:`osascript display notification` 在 **detached 无 TTY 后台进程**里 rc=0 可弹
(daemon 正是此形态)。
**但 codex 3 + 我实测的坑**:通知归属于 **`osascript` 这个 app**,权限/专注模式挂它身上;
被关时 **rc=0 照返回而你看不见** → "不能静默"变成假的**且表面满足**。
→ **绝不把 rc=0 当作"已送达"**;e2e 必须由 Yilun **肉眼确认**通知中心里有那条唯一 nonce。

**文案经 `osascript` 的 `run argv` 传参,不拼进脚本源**(codex Low:版本串来自外部可执行文件,
含引号会让通知静默失败)。

**每次事件恰好一条**(codex M6:防"自愈失败"与"ok→degraded"双发):
| 事件 | 通知 |
|---|---|
| 自愈成功 | 一条「已自愈 x→y」 |
| 自愈失败 | **一条合并的**「出站停摆 + 自检失败」 |
| 不可自愈的降级(含 identity_mismatch) | 一条「出站停摆」 |
| 后续恢复 | 一条「已恢复」 |
| 临时 `version_selfcheck` 态 | **不通知** |

**fail-open**:通知抛异常只吞+记日志,绝不影响门逻辑。

### 5. 只对 `version_mismatch` 自愈
`identity_mismatch`(换了 app/owner)**绝不自愈** —— 那是"谁在发"变了,必须停下问人。

### 6. `doctor` 重钉要读盘上最新 config(codex Low)
不能拿 `self.cfg` 的旧副本 copy-save,否则会覆盖并发写入的 allowlist 等值。

### 7. 版本 1.4.2 → **1.5.0**(minor:新行为,向后兼容)

## 明确不做(含 codex 的 CUT 清单)
- ❌ 删掉版本检查(防真实故障:CLAUDE.md 记 1.0.66 有信封回归;静默发错比停摆难查)
- ❌ 只比 major/minor 忽略 patch · ❌ identity_mismatch 自愈
- ❌ `selfcheck_chat_id` 配置项(硬编码常量即可)· ❌ recall 作为通过判据(整个砍掉)
- ❌ 卡片/reaction/post 兼容性矩阵 · ❌ 外层超时线程 / 独立自愈 worker
- ❌ 泛化地让 notify/StopFailure 绕过门(**另一个话题**;本轮用 osascript 旁路解决"通知"这半,
  不动出站门的安全语义)
- ❌ **飞书应急兜底**(codex r2 定:headless 不在范围;"通知被静音"从 osascript 的 rc 检测不到,
  要覆盖就得**无条件双发** = 经由可疑 CLI 的重复噪音)。→ **"macOS 通知已开启"= 运行前提**,
  已由 Yilun 肉眼确认弹出。
- ❌ 自动核验通知中心(私有库/UI 抓取)—— 用真人 e2e

## 测试(codex:收敛到约 6 条聚焦测试 + 5 个具名变异)
1. `version_mismatch` → 自愈调用**用 `--markdown`**(非 `--text`)且校验 `message_id`;成功 → 门 ok + 重钉
2. 自愈失败 → 门保持 degraded + **`cli_version` 未被改动** + 恰一条合并通知
3. **`identity_mismatch` → doctor 调用次数 == 0**(走真实 startup/tick 入口)
4. **持久化抑制跨 daemon 重建生效**:doctor 必须**失败** + 跨多个退避窗 + **新建第二个 Gate 实例**
   (codex:doctor 成功会让该变异隐形)
5. **自愈期间门已是非 ok**(防直发路径读到旧 `ok`)
6. 通知:跃迁才发、同状态不重复(**推进过每个退避 deadline**,codex:紧邻两次 tick 不评估 → 会假绿);
   通知抛异常 → **`DaemonCore.loop_iteration()` 连跑两次仍正常**(非只测 helper)
7. **绝不发到工作群**:构造一个 active 工作绑定,断言 doctor/send 调用为 0、且除测试群外无其它 chat_id

变异自证:纳入 identity 自愈 · 去掉持久化记忆 · 每 tick 都通知 · 通知异常炸循环 · 回退工作群
→ 各自必红。

## 流程
plan codex 收敛 → TDD → impl codex 收敛 → 全套(3.12+3.9)+ 变异矩阵 →
**真机 e2e:真造版本漂移**(改 config 的 cli_version)→ 看自愈真发生 + **Yilun 肉眼确认通知**
→ bump 1.5.0 + 双推。

---
## codex r5 findings 二分(2026-07-30,第 5 轮,已到元层)

**判为真闸(已修)**:
- **identity + version 同时不符时可能自愈** —— 直接违反"换了 app/owner 绝不自愈"这条**唯一硬
  安全性质**(拿身份存疑的 CLI 往外发)。已参数化覆盖,变异 S1「版本优先」实测变红。

**判为仪式(记档不修)** —— 服务的是"我们的变异矩阵是否完备",不是任何项目判据:
- `_heal_key` 只按 actual 的变异存活:**实现已经是 `pinned→actual`**,只是测试没钉死。
  风险是"将来有人改回去",不是当前缺陷。
- network fixture 带 `rc=4`/`code=503` 不够严格隔离:该分支另有覆盖,且 `type:network` 与
  数字码在真实信封里本就同时出现。
- identity_mismatch / 普通 degraded→ok 的"删掉通知"变异存活:通知是**旁路装饰**(fail-open),
  主路径(自愈成功/失败)已有条数+内容断言。

**叫停理由**:第 5 轮 findings 已全部落在「验证机械自身自洽」层,不再指向功能缺陷 ——
按 CLAUDE.md「验证机械会自我繁殖,该层占多数 findings = 叫停信号」处理。
